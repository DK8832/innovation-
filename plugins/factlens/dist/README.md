# Generated bundle

`server.mjs` is a generated bundle and is included in the submitted `FactLens-Share-0.1.0.zip`.

For a GitHub source checkout, regenerate it from the pinned lockfile:

```powershell
cd plugins/factlens
pnpm install --frozen-lockfile
pnpm run build
```

The build writes `dist/server.mjs`.
